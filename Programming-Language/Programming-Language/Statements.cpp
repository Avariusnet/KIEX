#include "Statements.h"

std::vector<std::string> Statements::statementValues({ "if" });

void Statements::findStatement(std::string line)
{
	for (auto& statmentValue : Statements::statementValues) 
	{

	}
}